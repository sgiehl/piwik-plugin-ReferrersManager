
<h3>List of all search engines known to piwik</h3>

<p>
    For region-specific search engines, the URL, "{}.example.com" will match
    any ISO3166-1 alpha2 country code against "{}".  Similarly, "example.{}"
    will match against valid country TLDs, but should be used sparingly to
    avoid false positives.
</p>

<table class="dataTable entityTable">

<thead>
<tr>
    <th>Name</th>
    <th>Url</th>
    <th>Parameter</th>
    <th>Backlink pattern</th>
    <th>Actions</th>
</tr>
</thead>
<tbody>
    {% for name,infos in searchEngineInfos %}
        {% for i,url in infos %}
        <tr>
            {% if i == 0 %}
            <td rowspan="{{ infos|length }}">
                <img src="{{ attribute(searchEngineLogos, name) }}">
                {{ name }}
            </td>
            {% endif %}
            <td>{{ url.url }}</td>
            <td>{{ url.parameters }}</td>
            <td title="{{ url.backlink }}">{{ url.backlink|truncate(25) }}</td>
            {% if i == 0 %}
            <td rowspan="{{ infos|length }}">
                <span class="link_but">
                    <img src="plugins/Morpheus/images/ico_delete.png" title="{{ 'General_Delete'|translate }}" border="0">
                    <span>{{ 'General_Delete'|translate }}</span>
                </span>
                <br />
                <span class="link_but">
                    <img src="plugins/Morpheus/images/ico_edit.png" title="{{ 'General_Edit'|translate }}" border="0">
                    <span>{{ 'General_Edit'|translate }}</span>
                </span>
            </td>
            {% endif %}
        </tr>
        {% endfor %}
    {% endfor %}
</tbody>
</table>